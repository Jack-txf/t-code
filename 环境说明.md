# 环境说明

## 项目概述

本项目是一个基于 DeepSeek API 的最小化 Agent 示例，演示从「本地工具调用」到「MCP（Model Context Protocol）集成」的逐步演进。

## 环境要求

- **Python 版本**：3.12+
- **虚拟环境**：项目根目录下已包含 `.venv/`
- **包管理**：`pyproject.toml` + `setuptools`

## 依赖说明

主要依赖已在 `pyproject.toml` 中声明：

| 包 | 用途 |
|---|---|
| `openai` | 调用 DeepSeek 兼容 API |
| `python-dotenv` | 加载 `.env` 环境变量 |
| `fastmcp` | MCP Server/Client 框架（`FastMCP`、`Client`、`StreamableHttpTransport`） |

安装方式：

```bash
pip install -e .
```

> 注：`fastmcp` 内部依赖 `mcp` 协议库，无需单独安装。

## 环境变量配置

项目通过根目录下的 `.env` 文件配置以下环境变量：

```
DEEPSEEK_BASE_URL=<DeepSeek API 地址>
DEEPSEEK_API_KEY=<你的 API 密钥>
DEEPSEEK_MODEL=<模型名称>
```

加载逻辑由 `load_env.py` 实现，程序启动时会自动读取 `.env` 并注入到当前进程的环境变量中（不会覆盖已存在的环境变量）。

### 验证环境变量

运行测试脚本：

```bash
python test.py
```

若配置正确，将输出三个环境变量的值。

## 项目结构

```
t-code/
├── .env                          # 环境变量配置
├── .venv/                        # Python 虚拟环境
├── load_env.py                   # .env 加载工具
├── test.py                       # 环境变量测试脚本
├── pyproject.toml                # 项目依赖与构建配置
├── 环境说明.md                    # 本文件
├── CLAUDE.md                     # Claude Code 工作指引
├── step-by-step-01/              # 阶段一：本地工具
│   ├── 架构.py                    # 骨架/模板
│   ├── simple-agentic-loop.py    # Agent 主程序（流式对话 + 本地工具调用）
│   └── tools.py                  # 本地工具定义（bash、read_file、write_file、list_dir）
├── step-by-step-02/              # 阶段二：MCP 基础集成
│   ├── simple-agent-mcp.py       # MCP 客户端骨架
│   ├── config/
│   │   ├── __init__.py
│   │   └── load_tcode_config.py  # ~/.tcode.json 配置管理
│   ├── mcp_client/
│   │   ├── __init__.py
│   │   ├── tcode_mcp_client.py   # fastmcp.Client 直连测试
│   │   └── tcode_mcp_client_upper.py  # TCodeMCPClient 封装类（配置驱动）
│   └── remote_mcp_server/
│       └── FastMcp_Server.py     # FastMCP HTTP Server（端口 9000）
└── step-by-step-02-reactor/      # 阶段二重构：多 Server MCP Agent
    ├── mcp_code_agent.py         # 基于 MyMCPClient 的完整 Agent
    └── mymcp/
        ├── __init__.py
        ├── tmcp_client.py        # 多 Server MCP 客户端（自动路由、并发拉取工具）
        └── tmcp_server.py        # Streamable-HTTP MCP Server（端口 9000）
```

## 如何运行

### 1. 激活虚拟环境（Windows）

```bash
.venv\Scripts\activate
```

### 2. 确保 `.env` 文件已正确配置

### 3. 启动 MCP Server（二选一）

```bash
# 基础 HTTP Server（step-by-step-02 用）
python step-by-step-02\remote_mcp_server\FastMcp_Server.py

# Streamable-HTTP Server（step-by-step-02-reactor 用）
python step-by-step-02-reactor\mymcp\tmcp_server.py
```

### 4. 运行 Agent（另一个终端）

```bash
# 阶段一：本地工具
python step-by-step-01\simple-agentic-loop.py

# 阶段二：MCP 客户端测试
python step-by-step-02\mcp_client\tcode_mcp_client.py

# 阶段二重构：完整 MCP Agent
python step-by-step-02-reactor\mcp_code_agent.py
```

## 核心逻辑简介

### 阶段一：本地工具 Agent

- **`simple-agentic-loop.py`**：实现了一个 REPL 式交互循环。通过流式调用 LLM，解析模型返回的文本和工具调用请求，执行本地工具后将结果回传给模型，最多循环 20 轮。
- **`tools.py`**：定义了 4 个本地工具及其 JSON Schema，供 LLM 的 Function Calling 使用。

### 阶段二：MCP 集成

- **`FastMcp_Server.py`**：使用 `FastMCP` 将同样的 4 个工具暴露为 HTTP 服务，供外部 MCP 客户端调用。
- **`tcode_mcp_client_upper.py`**：封装了 `fastmcp.Client` 的配置文件驱动用法，从 `~/.tcode.json` 读取服务器配置，通过 `async with` 管理连接生命周期。

### 阶段二重构：多 Server MCP Agent

- **`tmcp_server.py`**：工具返回结构化 `dict`，并增加了 Resources 和 Prompts 定义。
- **`tmcp_client.py`**：支持连接多个 MCP Server，并发拉取工具列表；`call_tool` 支持自动路由到拥有该工具的 Server。
- **`mcp_code_agent.py`**：完整的 Agent 主程序，将 MCP 工具 schema 转换为 OpenAI function-calling 格式，流式对话中自动调用 MCP 工具。

## 注意事项

- **MCP Server 必须先启动**，Agent 才能正常拉取工具列表和调用工具。
- `fastmcp.Client` 必须在 `async with` 上下文管理器中使用，`Client(...)` 仅创建实例，不会建立网络连接。
- 工具输出超过 4000 字符时会被截断，避免撑爆上下文窗口。
- 本项目无测试框架、无 linter、无构建流程，属于教学/演示性质的轻量代码。